# Skills

## Languages & Frameworks

- **Go** — primary language, 6+ years. Hexagonal/DDD architecture, scalable backend services, e-commerce systems, LLM agent backends.
- **Python** — LLM agent tooling, FastAPI backends, data/AI scripting.
- **TypeScript / Next.js** — frontend work (e.g. this portfolio's chat UI).
- **Rust** — ultra-low-latency systems (e.g. hft_rtb, a market-data processing stack with single-digit-millisecond budgets).
- Also has worked with Scala, C++.

## AI / LLM Engineering

- Multi-turn ReAct-style agent loops: tool use, retrieval, and reasoning orchestration for real conversational queries.
- LLM-agnostic backend architecture: multi-provider routing (fast intent classification vs. deep reasoning vs. safety checks), enabling model evaluation and cost/quality tradeoffs without rearchitecting.
- Systematic prompt engineering: modular prompt composition, token-budget discipline, behavior-preserving refactors validated against regression suites.
- Guardrail and safety layers (policy + LLM-based checks) for compliant, on-brand agent behavior at scale.
- Evaluation infrastructure: golden test sets, quality gates — treating agent behavior as testable, measurable software.
- Retrieval/search relevance, hybrid (keyword + vector) search design, knowledge grounding / RAG to reduce hallucination.
- Tool-calling agent design, MCP (Model Context Protocol) servers.

## Infrastructure & Ops

- Docker, Docker Compose, CI/CD pipelines (GitHub Actions).
- Self-hosted deployment: VPS management, reverse proxies (Caddy), Cloudflare (DNS, Tunnels), Let's Encrypt/automatic HTTPS.
- Postgres, pgvector, Supabase (Auth, Postgres, Row Level Security).
- Service scalability and reliability engineering for high-traffic production systems.

## Product & Collaboration

- User-driven product development: A/B testing, experimental validation of features.
- Cross-functional stakeholder integration in fast-paced, high-traffic environments.
- Owning the full lifecycle of a feature: problem framing, system design, evaluation instrumentation, iterating from production signal.
